# 🚀 交易机器人 AWS 部署完整指南

> **服务器 IP**: `35.74.81.38` | **SSH 密钥**: `~/pem/jpbian.pem` | **Web 端口**: `7860`

## 📋 目录
- [最简单的部署方法（推荐）](#1-最简单的部署方法推荐)
- [备选方案：本地文件上传](#2-备选方案本地文件上传)
- [安全组配置](#3-安全组配置)
- [常见问题](#4-常见问题)

---

## 1. 最简单的部署方法（推荐）✨

### 步骤 1：打开 EC2 浏览器终端

1. 登录 [AWS 控制台](https://console.aws.amazon.com/ec2/)
2. 进入 **EC2** 服务
3. 找到你的实例（IP：`35.74.81.38`）并选中
4. 点击右上角的 **Connect**（连接）按钮
5. 选择 **EC2 Instance Connect** 标签页
6. 点击 **Connect** 连接按钮
7. 现在你应该能看到一个黑色的终端窗口了！

### 步骤 2：运行部署脚本

在打开的终端中，复制粘贴以下完整内容并按回车：

```bash
mkdir -p ~/trading-bot && cd ~/trading-bot

# 创建 requirements.txt
cat > requirements.txt << 'EOF'
ccxt>=4.0.0
pandas>=2.0.0
python-dotenv>=1.0.0
flask>=2.3.0
requests>=2.31.0
EOF

# 创建 Dockerfile
cat > Dockerfile << 'EOF'
FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc curl && rm -rf /var/lib/apt/lists/*
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py .
COPY new_strategy_bot.py .
RUN mkdir -p /app/data
EXPOSE 7860
ENV PYTHONUNBUFFERED=1
ENV DATA_DIR=/app/data
CMD ["python", "app.py"]
EOF

# 创建 docker-compose.yml
cat > docker-compose.yml << 'EOF'
version: '3.8'
services:
  trading-bot:
    build: .
    container_name: trading-bot
    restart: unless-stopped
    ports:
      - "7860:7860"
    volumes:
      - bot-data:/app/data
      - ./binance_config.env:/app/binance_config.env:ro
    environment:
      - PYTHONUNBUFFERED=1
      - DATA_DIR=/app/data
volumes:
  bot-data:
EOF

# 创建配置文件模板
cat > binance_config.env.template << 'EOF'
OKX_API_KEY=your_api_key_here
OKX_SECRET_KEY=your_secret_key_here
OKX_PASSPHRASE=your_passphrase_here
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
EOF
cp binance_config.env.template binance_config.env

# 创建 app.py
cat > app.py << 'EOF'
import os
import time
import ccxt
import json
from datetime import datetime, timedelta, timezone
from dotenv import load_dotenv
from flask import Flask, jsonify
from threading import Thread

from new_strategy_bot import (
    run_new_strategy_check, SYMBOL, LEVERAGE, RSI_LENGTH,
    MACD_FAST, MACD_SLOW, MACD_SIGNAL, position_tracker,
    last_execution, TARGET_MARGIN,
    is_market_active, get_current_session
)

load_dotenv('binance_config.env')
print("=== Hugging Face Spaces Bot 启动 (OKX + 1H/4H 策略 ===")

exchange = ccxt.okx({
    'apiKey': os.getenv('OKX_API_KEY'),
    'secret': os.getenv('OKX_SECRET_KEY'),
    'password': os.getenv('OKX_PASSPHRASE'),
    'enableRateLimit': True,
    'options': {'defaultType': 'swap'}
})

contract_info_cache = {}
app = Flask(__name__)

def fetch_contract_info():
    global contract_info_cache
    try:
        balance = exchange.fetch_balance()
        contract_info_cache = {
            'timestamp': datetime.now(timezone.utc).astimezone(timezone(timedelta(hours=8))).strftime('%Y-%m-%d %H:%M:%S'),
            'exchange': 'OKX', 'symbol': SYMBOL, 'balance': balance, 'error': None
        }
        try:
            positions = exchange.fetch_positions([SYMBOL])
            if positions: contract_info_cache['positions'] = positions
        except Exception as e: contract_info_cache['positions_error'] = str(e)
        return True
    except Exception as e: contract_info_cache['error'] = str(e); return False

@app.route('/')
def index():
    if not last_execution.get('timestamp'):
        try: run_new_strategy_check(position_tracker.get('position'), exchange, SYMBOL, MACD_FAST, MACD_SLOW, MACD_SIGNAL, RSI_LENGTH, False, False)
        except: pass
    return 'Trading Bot Running'

@app.route('/health')
def health(): return 'OK', 200

def run_bot():
    last_1h_check, last_4h_check = None, None
    while True:
        now_utc = datetime.now(timezone.utc)
        now_cst = now_utc.astimezone(timezone(timedelta(hours=8)))
        m, s = now_cst.minute, now_cst.second
        active = is_market_active()
        has_position = bool(position_tracker.get('position'))
        
        if has_position:
            if m % 4 == 0 and 10 <= s <= 30:
                if last_4h_check is None or (now_utc - last_4h_check) >= timedelta(minutes=239):
                    run_new_strategy_check(position_tracker.get('position'), exchange, SYMBOL, MACD_FAST, MACD_SLOW, MACD_SIGNAL, RSI_LENGTH, True, False)
                    last_4h_check = now_utc
        else:
            if active and m == 0 and 10 <= s <= 30:
                if last_1h_check is None or (now_utc - last_1h_check) >= timedelta(minutes=59):
                    run_new_strategy_check(position_tracker.get('position'), exchange, SYMBOL, MACD_FAST, MACD_SLOW, MACD_SIGNAL, RSI_LENGTH, False, True)
                    last_1h_check = now_utc
        
        if m == 0 and 10 <= s <= 20:
            try: run_new_strategy_check(position_tracker.get('position'), exchange, SYMBOL, MACD_FAST, MACD_SLOW, MACD_SIGNAL, RSI_LENGTH, False, False)
            except: pass
        time.sleep(1)

def update_contract_info():
    while True: try: time.sleep(30); fetch_contract_info(); except: pass

if __name__ == '__main__':
    fetch_contract_info()
    Thread(target=run_bot, daemon=True).start()
    Thread(target=update_contract_info, daemon=True).start()
    app.run(host='0.0.0.0', port=7860)
EOF

# 创建 new_strategy_bot.py
cat > new_strategy_bot.py << 'EOF'
#!/usr/bin/env python3
import os, json, logging, time, ccxt, pandas as pd, requests
from datetime import datetime, timedelta, timezone
from dotenv import load_dotenv
from pathlib import Path

load_dotenv('binance_config.env')

SYMBOL = 'BTC-USDT-SWAP'
TARGET_MARGIN = 20
LEVERAGE = 7
RSI_LENGTH = 9
MACD_FAST = 9
MACD_SLOW = 21
MACD_SIGNAL = 60
RSI_OVERBOUGHT = 70
RSI_OVERSOLD = 30

DATA_DIR = Path(os.getenv('DATA_DIR', '.'))
DATA_DIR.mkdir(exist_ok=True)
STATE_FILE = DATA_DIR / 'state.json'

logging.basicConfig(level=logging.INFO, format='%(asctime)s [%(levelname)s] %(message)s', handlers=[logging.FileHandler(DATA_DIR / 'bot.log', encoding='utf-8'), logging.StreamHandler()])
log = logging.getLogger(__name__)

TELEGRAM_TOKEN = os.getenv('TELEGRAM_TOKEN')
TELEGRAM_CHAT_ID = os.getenv('TELEGRAM_CHAT_ID')
log.info("=== 1H反转 + 4H平仓 策略已启动 ===")

def send_telegram_message(message):
    if not TELEGRAM_TOKEN or not TELEGRAM_CHAT_ID: return
    try:
        url = f"https://api.telegram.org/bot{TELEGRAM_TOKEN}/sendMessage"
        requests.get(url, params={'chat_id': TELEGRAM_CHAT_ID, 'text': message, 'parse_mode': 'Markdown'}, timeout=10)
    except: pass

def is_market_active(): now_jst = datetime.now(timezone(timedelta(hours=9))); return (8 <= now_jst.hour < 24) or (0 <= now_jst.hour < 2)
def get_current_session(): now_jst = datetime.now(timezone(timedelta(hours=9))); h=now_jst.hour; return '亚洲' if 8<=h<17 else ('欧美' if 17<=h<24 or 0<=h<2 else '休眠')

exchange = ccxt.okx({
    'apiKey': os.getenv('OKX_API_KEY'), 'secret': os.getenv('OKX_SECRET_KEY'), 'password': os.getenv('OKX_PASSPHRASE'),
    'enableRateLimit': True, 'options': {'defaultType': 'swap', 'marginMode': 'isolated'}
})

_DEFAULT_TRACKER = {'entry_price': None, 'entry_time': None, 'position': None, 'entry_1h_macd_direction': None, 'last_1h_macd_direction': None, 'open_contracts': None, 'trade_history': [], 'last_trade_loss': False}

def save_state():
    try: data={k:v.isoformat() if isinstance(v,datetime) else v for k,v in position_tracker.items()}; STATE_FILE.write_text(json.dumps(data, ensure_ascii=False, indent=2))
    except Exception as e: log.warning(f"保存状态失败: {e}")
def load_state():
    if STATE_FILE.exists():
        try: data=json.loads(STATE_FILE.read_text()); data['entry_time']=datetime.fromisoformat(data['entry_time']) if data.get('entry_time') else data['entry_time']; return data
        except: pass
    return dict(_DEFAULT_TRACKER)
position_tracker = load_state()
log.info(f"状态加载完成: position={position_tracker.get('position')}")
last_execution = {'timestamp': None, 'current_price': None, 'macd_4h_trend': None, 'macd_1h_trend': None}
_contract_size_cache = {}

def get_contract_size():
    if 'size' in _contract_size_cache: return _contract_size_cache['size']
    try: markets=exchange.load_markets(); market=markets.get(SYMBOL) or markets.get(SYMBOL.replace('-','/')); size=float(market.get('contractSize',0.01))
    except: size=0.01
    _contract_size_cache['size']=size; return size
def calculate_contracts(price): return round(TARGET_MARGIN * LEVERAGE / (price * get_contract_size()), 4)
def open_position(direction, price, entry_1h_dir):
    try:
        contracts=calculate_contracts(price); side='buy' if direction=='long' else 'sell'
        log.info(f"⬆️ 尝试开仓 {direction.upper()} | 价格: ${price:.2f} | 张数: {contracts}")
        exchange.create_order(symbol=SYMBOL, type='market', side=side, amount=contracts, params={'tdMode':'isolated','posSide':direction})
        position_tracker.update({'entry_price': float(price), 'entry_time': datetime.now(timezone.utc), 'position': direction, 'open_contracts': contracts, 'entry_1h_macd_direction': entry_1h_dir})
        save_state()
        msg=f"✅ 开仓成功\n📈 方向: {direction.upper()}\n💰 价格: ${price:.2f}\n📦 张数: {contracts}"
        log.info(msg); send_telegram_message(msg); return True
    except Exception as e: log.error(f"❌ 开仓失败: {e}"); return False
def close_position(reason=''):
    try:
        direction=position_tracker.get('position'); contracts=position_tracker.get('open_contracts')
        if not direction or not contracts: return True
        side='sell' if direction=='long' else 'buy'
        exchange.create_order(symbol=SYMBOL, type='market', side=side, amount=contracts, params={'tdMode':'isolated','posSide':direction,'reduceOnly':True})
        position_tracker.update({'entry_price': None, 'entry_time': None, 'position': None, 'entry_1h_macd_direction': None, 'open_contracts': None})
        save_state()
        msg=f"🔔 平仓成功\n📉 方向: {direction.upper()}\n📋 原因: {reason}"
        log.info(msg); send_telegram_message(msg); return True
    except Exception as e: log.error(f"❌ 平仓失败: {e}"); return False

def fetch_klines(exch, symbol, timeframe, limit=150):
    ohlcv=exch.fetch_ohlcv(symbol, timeframe, limit=limit)
    df=pd.DataFrame(ohlcv, columns=['timestamp','open','high','low','close','volume'])
    df['timestamp']=pd.to_datetime(df['timestamp'], unit='ms', utc=True)
    for c in ['open','high','low','close']: df[c]=pd.to_numeric(df[c])
    return df
def calculate_macd_okx(prices):
    s=pd.Series(prices); ema_fast=s.ewm(span=MACD_FAST, adjust=False).mean(); ema_slow=s.ewm(span=MACD_SLOW, adjust=False).mean()
    macd_line=ema_fast-ema_slow; signal_line=macd_line.ewm(span=MACD_SIGNAL, adjust=False).mean(); histogram=(macd_line-signal_line)*2
    return histogram.values
def calculate_indicators(df):
    if len(df)<50: return df
    df['macd_hist']=calculate_macd_okx(df['close'])
    return df
def get_macd_trend(df, idx=-1):
    if df is None or len(df) < abs(idx) or 'macd_hist' not in df.columns: return None
    cur=df['macd_hist'].iloc[idx]
    if pd.isna(cur): return None
    return 'long' if cur>0 else 'short'
def run_new_strategy_check(_, exch, symbol, fast, slow, signal, rsi_len, is_4h_check=False, is_1h_check=False):
    global last_execution
    now_str=datetime.now(timezone.utc).astimezone(timezone(timedelta(hours=8))).strftime('%Y-%m-%d %H:%M:%S')
    active=is_market_active(); session=get_current_session(); current_price=None
    try: ticker=exch.fetch_ticker(symbol); current_price=float(ticker['last']); except: pass
    df1h, df4h = None, None
    try: df1h=fetch_klines(exch, symbol, '1h', 100); df1h=calculate_indicators(df1h); except: pass
    try: df4h=fetch_klines(exch, symbol, '4h', 60); df4h=calculate_indicators(df4h); except: pass
    trend_1h=get_macd_trend(df1h); trend_4h=get_macd_trend(df4h)
    log.info(f"[{now_str}] {session} | 1H:{trend_1h} | 4H:{trend_4h} | 活跃:{active}")
    if is_4h_check and position_tracker.get('position'):
        pos=position_tracker.get('position')
        if pos and trend_4h and pos != trend_4h: close_position(f"4H趋势({trend_4h}) 与持仓方向({pos}) 不一致"); last_execution['close_triggered']=True
    if is_1h_check and active and not position_tracker.get('position') and trend_1h and trend_4h:
        prev_1h=get_macd_trend(df1h, -2)
        if prev_1h and trend_1h != prev_1h:
            reversal_recent=False
            prev_2h=get_macd_trend(df1h,-3) if df1h is not None and len(df1h)>=3 else None
            prev_3h=get_macd_trend(df1h,-4) if df1h is not None and len(df1h)>=4 else None
            if df1h is not None and len(df1h)>=4: reversal_recent=(prev_1h != prev_2h) or (prev_2h != prev_3h and prev_1h == prev_2h)
            else: reversal_recent=True
            log.info(f"📋 反转分析: 当前={trend_1h} vs 前一根={prev_1h}")
            if reversal_recent and trend_4h == trend_1h:
                log.info(f"⚡ 1H反转 + 4H同向 → 执行开仓")
                if current_price: open_position(trend_1h, current_price, trend_1h)
            elif not reversal_recent: log.info(f"⚠️ 反转超过2根K线")
            elif trend_4h != trend_1h: log.info(f"⚠️ 4H方向不一致")
    last_execution.update({'timestamp': now_str, 'current_price': current_price, 'macd_4h_trend': trend_4h, 'macd_1h_trend': trend_1h})
    save_state()
EOF

echo "✅ 文件创建完成！"

# 安装 Docker
if ! command -v docker &> /dev/null; then
    echo "🐳 安装 Docker..."
    curl -fsSL https://get.docker.com -o get-docker.sh
    sudo sh get-docker.sh
    sudo usermod -aG docker $USER
    echo "✅ Docker 安装成功！"
else
    echo "✅ Docker 已存在"
fi

echo ""
echo "========================================"
echo "🎉 第一阶段完成！"
echo "========================================"
echo ""
echo "请按以下步骤继续："
echo ""
echo "1. 退出当前会话：执行 exit"
echo "2. 重新通过 EC2 Instance Connect 连接"
echo ""
echo "重新连接后执行："
echo "   cd ~/trading-bot"
echo ""
echo "然后配置 API 密钥："
echo "   nano binance_config.env"
echo "   (修改密钥后 Ctrl+O 回车保存，Ctrl+X 退出)"
echo ""
echo "最后启动机器人："
echo "   docker compose up -d --build"
echo ""
echo "查看日志："
echo "   docker compose logs -f"
echo ""
echo "访问地址：http://35.74.81.38:7860"
echo "========================================"
```

**注意：** 这一步可能需要1-2分钟，请耐心等待！

### 步骤 3：退出并重新连接

脚本执行完成后，在终端中输入：

```bash
exit
```

然后按照步骤 1 的方法重新连接到 EC2 实例。

### 步骤 4：配置 API 密钥

重新连接后，执行：

```bash
cd ~/trading-bot
nano binance_config.env
```

在打开的编辑器中，将：
- `your_api_key_here` 改为你的 OKX API Key
- `your_secret_key_here` 改为你的 OKX Secret Key
- `your_passphrase_here` 改为你的 OKX Passphrase

修改完成后：
1. 按 `Ctrl + O`（字母O）保存
2. 按 `Enter` 确认
3. 按 `Ctrl + X` 退出

### 步骤 5：启动机器人 🚀

```bash
cd ~/trading-bot
docker compose up -d --build
```

**注意：** 第一次构建需要5-10分钟，因为需要下载依赖。

### 步骤 6：查看运行状态

```bash
# 查看实时日志
docker compose logs -f

# 或者查看最近的100行
docker compose logs --tail=100
```

### 步骤 7：访问 Web 界面

在浏览器中打开：

```
http://35.74.81.38:7860
```

---

## 2. 备选方案：本地文件上传

如果上面的方法太麻烦，你也可以：

### 步骤 1：确认你的 SSH 密钥位置

你提到密钥在 `/Users/johnsontang/pem`，让我们先看看：

```bash
ls -la ~/pem
```

请告诉我看到了什么文件名？

---

### ✅ 成功连接 AWS 的 SSH 命令

**重要：** 使用 `-o StrictHostKeyChecking=no` 参数避免首次连接时的确认提示。

#### 查看日志
```bash
ssh -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem ubuntu@35.74.81.38 "cd ~/trading-bot && docker compose logs --tail 150"
```

#### 上传文件
```bash
scp -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem /Users/johnsontang/work/bitbuy/newst/trading_bot/new_strategy_bot.py ubuntu@35.74.81.38:~/trading-bot/
```

#### 重启容器
```bash
ssh -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem ubuntu@35.74.81.38 "cd ~/trading-bot && docker compose down && docker compose up -d"
```

#### 实时查看日志
```bash
ssh -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem ubuntu@35.74.81.38 "cd ~/trading-bot && docker compose logs -f"
```

#### 查看容器状态
```bash
ssh -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem ubuntu@35.74.81.38 "docker ps"
```

#### 查看数据文件
```bash
ssh -o StrictHostKeyChecking=no -i ~/pem/jpbian.pem ubuntu@35.74.81.38 "cd ~/trading-bot && cat data/state.json"
```

---

### 📝 常用服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | `35.74.81.38` |
| SSH 用户 | `ubuntu` |
| SSH 密钥 | `~/pem/jpbian.pem` |
| Web 端口 | `7860` |
| 工作目录 | `~/trading-bot` |
| 日志命令 | `docker compose logs --tail 100` |

---

## 3. 安全组配置 ⚠️

**重要：** 在访问 Web 界面前，请确保你的 EC2 安全组已开放 7860 端口：

1. 进入 EC2 控制台 → 你的实例 → 安全 → 安全组
2. 点击安全组名称进入配置
3. 点击 "入站规则" → "编辑入站规则"
4. 添加规则：
   - **类型：** 自定义 TCP
   - **端口范围：** 7860
   - **来源：** 0.0.0.0/0 （或限制为你自己的 IP）
5. 点击 "保存规则"

---

## 4. 常见问题 ❓

### Q: Docker 命令提示权限不足？
A: 需要退出并重新登录 EC2，使 docker 用户组生效。

### Q: 构建时间太长？
A: 第一次构建需要下载依赖，以后会很快。

### Q: 如何停止机器人？
A:
```bash
cd ~/trading-bot
docker compose down
```

### Q: 如何重启机器人？
A:
```bash
cd ~/trading-bot
docker compose restart
```

### Q: 如何查看数据？
A:
```bash
cd ~/trading-bot
# 查看状态文件
cat data/state.json
# 查看日志
cat data/bot.log
```

---

## 🎯 部署成功！

如果一切顺利，你的交易机器人现在已经在 AWS 上 24/7 运行了！

有任何问题随时告诉我！
